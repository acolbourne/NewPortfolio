// -> Constants -> Type Declarations
export type websiteSettings = typeof websiteSettings;
export type navigationItems = typeof navigationItems;

// -> Constants -> Website
export const websiteSettings = {

    'name': 'Andrew C - Dev.',
    'description': 'Dev.'

} as const;

// -> Constants -> Navigation Items
export const navigationItems = [
    
    {
        id: 1,
    }

] as const;